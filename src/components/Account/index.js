import React from 'react';

const Account = () => (
    <div>
        <h1>This is Account Page</h1>
        <h2>Here You can resetNow, all these routes need to be accessible to the user. First, you need a router for your web application, which is responsible to map routes to React components. React Router is a popular package to enable routing, so install it on the command line:

 or change a password.</h2>
    </div>
);

export default Account;